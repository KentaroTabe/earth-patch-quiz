node_modules/
.DS_Store

# 取得パイプラインの作業ディレクトリ（生成物。コミットしない）
work/

# 問題画像。配信は R2 から行うので Git に入れない（仕様 §4.1）。
# 手元に用意し直すときは npm run fetch。manifest.js だけは残す。
img/*
!img/manifest.js

# エディタ・ツールのローカル設定
.claude/

# 秘密情報。雛形は .env.local.example のほう
.env
.env.local
.env.*.local
