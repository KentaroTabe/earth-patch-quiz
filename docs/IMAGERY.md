# 画像の取得と差し替え

仕様 §4.2 / §5.6 / §12 の実装メモ。

---

## 1. いま使っている取得元

**NASA GIBS の Landsat 年次無雲モザイク**（`config/sources.json` の `gibs-landsat-annual`）。

| | |
| --- | --- |
| レイヤ | `Landsat_WELD_CorrectedReflectance_TrueColor_Global_Annual` |
| 分解能 | 30 m/px |
| 時期 | 年次モザイク（TIME=`2000-12-01`）。**中身は 2000 年前後の画像** |
| ライセンス | 米国政府の著作物。出典表記のうえ自由に使える |
| 認証 | 不要 |

仕様 §5.6 のとおり年次の無雲モザイクを使っているので、単日のシーンを選んで雲を弾く工程は要らない。

### なぜこれを選んだか

仕様 原則1 は「一次配布元の衛星画像のみ。NASA と Copernicus に限る」。認証情報なしで一次配布元から 30m の全球無雲モザイクが取れるのは、いまのところ GIBS だけだった。

検討して見送ったもの。

| 候補 | 見送った理由 |
| --- | --- |
| EOX Sentinel-2 cloudless（10m） | CC BY-NC-SA 4.0 の非商用限定。かつ配布元が EOX（第三者）なので原則1に厳密には反する |
| Copernicus Data Space / Sentinel-2（10m） | 認証情報が要る。取れ次第こちらに移す（`cdse-sentinel2` として枠だけ用意済み） |
| GIBS HLS L30/S30（30m・日次） | 日次なので雲が乗る。仕様 §5.6 が避けたい工程が復活する |
| GIBS MODIS/VIIRS（250m） | 2000 km² の枠でも 179px にしかならない |

**仕様 付録の未確定事項 #1（無雲モザイクの提供元とライセンス）は、GIBS を選ぶことで当面決着させた。** 商用利用の制約が無く、出典表記だけで足りる。

### この取得元の弱点

2000 年前後の画像なので、**それ以降にできた地物は写らない。** 確認済みの例。

- パーム・ジュメイラ（ドバイ、2001年着工）→ 写らない
- 三峡ダムの湛水（2003年〜）→ 写らない

また、次のような場所では画質が実用に足りなかった。29 枚取って 19 枚を落としている（§4）。

- 雪氷・塩原 … 白飛びする（ヴァトナヨークトル、ウユニ）
- 湿潤な熱帯 … 走査線状の欠測が出る（マナウス、レンソイス）
- 高緯度の山岳 … 雪と岩の見分けがつかない（エベレスト）

**乾燥地・海岸線・火山はよく写る。** 現在の20問がそちらに偏っているのはこのため。

---

## 2. 取得のしかた

```bash
npm run fetch:dry                     # 通信せず、叩く URL だけ確認する
npm run fetch                         # まだ無いものだけ取得する
npm run fetch -- --force              # 取り直す
npm run fetch -- --only=richat,fuji   # id を指定する
npm run fetch -- --prune              # 不採用（adopted: false）の画像を消す
npm run fetch -- --source=gibs-modis-truecolor   # 取得元を差し替える
```

`data/questions.js` の `answer.lat` / `answer.lon` / `frame.areaKm2` を読んで、WMS の GetMap で1枠を1リクエストで取る。タイルの合成をしないので画像ライブラリが要らない。

出力は `img/q/<id>.jpg` と `img/manifest.js`（自動生成・手で編集しない）。

### 画素数は枠で決まる

引き伸ばさない（仕様 §5.4）。30 m/px なので、

| 枠 | 一辺 | 画素数 |
| --- | --- | --- |
| 200 km² | 14.1 km | 471 px |
| 500 km² | 22.4 km | 745 px |
| 1000 km² | 31.6 km | 1054 px |
| 2000 km² | 44.7 km | 1400 px（上限で頭打ち） |

50 km² / 100 km² の枠は 236 / 333 px にしかならず、30m の取得元では実用にならない。**小さい枠を使いたければ 10m の取得元が要る。**

---

## 3. 目視の手順

仕様 §13 のとおり、機械は「人が見る順番を並べ替える」だけで、採否は人が決める。

```bash
npm run fetch     # 候補をまとめて取る
npm run sheet     # work/contact-sheet.html を書き出す
npm run serve     # http://localhost:4173/work/contact-sheet.html で開く
```

見るのは3点。

1. **狙った地物が枠に収まっているか。** 収まっていなければ `frame.areaKm2` か中心座標を直して取り直す。
2. **欠測や白飛びが無いか。** 走査線状の縞、真っ白な面、斑点。
3. **見たままを一文で言えるか。** 言えないなら `headline` が書けないということなので落とす。

採否は `data/questions.js` の `adopted` に書く。

- `true` … 採用
- `false` … 不採用。**理由を `reject` に書く**
- 書かない … 未決定

**不採用を消さないこと。** 消すと取得のたびに同じ画像が戻ってくる（仕様 §6.1）。

---

## 4. 落とした19件と理由

| id | 場所 | 理由 |
| --- | --- | --- |
| uyuni | ウユニ塩原 | 年次モザイクでは白飛びして塩原の平坦さが出ない |
| vatnajokull | ヴァトナヨークトル | 雪氷で全面が白飛びする |
| everest | エベレスト | 雪と岩が入り乱れ、どれが頂か分からない |
| manaus | ネグロ川の合流点 | 走査線状の欠測が画面を覆っている |
| lencois | レンソイス・マラニャンセス | 走査線状の欠測が画面を覆っている |
| natron | ナトロン湖 | 水面全体に斑点状の欠測が出ている |
| kondyor | コンダー山塊 | 枠の右端に走査線状の欠測が残る |
| chuquicamata | チュキカマタ銅山 | 30m ではコントラストが立たず、穴の輪郭が読めない |
| okavango | オカバンゴ・デルタ | 扇の全体が枠に入らず、まだら模様にしか見えない |
| danube-delta | ドナウ・デルタ | 三角州の内側だけが写り、扇の形にならない |
| balkhash | バルハシ湖 | 枠が湖の内側に収まってしまい、水面しか写らない |
| panama | パナマ運河 | 2000km² まで広げても運河の筋が読めず、熱帯の湖にしか見えない |
| etosha | エトーシャ・パン | 塩湖の縁が枠に入らず、白い面が広がるだけになる |
| salar-atacama | アタカマ塩湖 | 塩原の面積が大きすぎて、枠の中は一様な平地になる |
| lake-eyre | エア湖 | 一様な砂色で手がかりが何も出ない |
| dasht-e-kavir | ダシュテ・カヴィール | 靄がかかってコントラストが立たない |
| danakil | ダナキル低地 | 全体が白っぽく、地溝の段差が読めない |
| aral | アラル海 | 水際だけが写り、ただの海岸線に見える |
| ounianga | ウニアンガ湖群 | 湖が枠の一部にしか写らず、周りの砂に埋もれる |

**枠が大きすぎて全体が入らない**（オカバンゴ、ドナウ、パナマ、バルハシ）が5件。仕様 §5.4 の SIZES は上限 2000 km²（一辺 45km）なので、それより大きな地物は今の枠では扱えない。SIZES を伸ばすか、その手の地物を候補から外すかは決めていない。

---

## 5. Cloudflare R2 へ移す

いまは画像を **リポジトリに同梱**している（`img/q/`、20枚で約4MB）。Cloudflare のアカウントを作ったら R2 へ移す。

仕様 §4.1 は画像を Git に入れないとしている。同梱しているのは Cloudflare のアカウントがまだ無く、それでも動く状態にしておきたかったから。仕様 §11 の見積もりでも100問25MBまでは「どこでも可」なので、当面は問題にならない。**500問を超えるあたりで必ず R2 へ移すこと。**

### 手順

1. R2 にバケットを作る。
2. **バケットにカスタムドメインを割り当てる。** `r2.dev` の公開エンドポイントは検証用で、レート制限を超えると 429 が返る。ここを忘れると人が集まった瞬間に画像だけ表示されなくなる（仕様 §4.2）。
3. バケットを公開読み取りにする。書き込みは API トークンのみ。
4. `config/pipeline.json` の `r2` を埋める。

   ```json
   "r2": {
     "enabled": true,
     "accountId": "xxxxxxxx",
     "bucket": "earth-patch",
     "region": "auto",
     "prefix": "q/",
     "publicBase": "https://img.example.com/"
   }
   ```

5. API トークンを環境変数で渡す。**`.env` やコードに書かない。**

   ```bash
   export R2_ACCESS_KEY_ID=...
   export R2_SECRET_ACCESS_KEY=...
   ```

6. アップロードする。

   ```bash
   npm run fetch -- --force
   ```

7. `assets/config.js` の `imageBase` を差し替える。**触るのはこの1行だけ。**

   ```js
   imageBase: 'https://img.example.com/q/',
   ```

8. `img/q/` をリポジトリから外し、`.gitignore` に足す。

アップロードは S3 互換 API に SigV4 で署名して PUT している（`scripts/lib/r2.mjs`）。外部ライブラリは使っていない。

---

## 6. 取得元を足す

`config/sources.json` に1件足すだけ。スクリプトは触らなくてよい。

```json
"あなたのソース名": {
  "label": "表示名",
  "protocol": "wms",
  "endpoint": "https://…",
  "wmsVersion": "1.1.1",
  "srs": "EPSG:4326",
  "layers": { "truecolor": "レイヤ名" },
  "time": "2024-01-01",
  "metersPerPixel": 10,
  "maxRequestPx": 4096,
  "sensor": "s2",
  "credit": "出典表記",
  "license": "…",
  "commercialUse": true,
  "requiresApproval": false,
  "reflectanceLinear": true
}
```

`reflectanceLinear` は弁別性の計算に効く（docs/SCORING.md §2）。表示用に伸張された合成画像しか出さないソースは `false` にする。

`protocol` が `wms` 以外のものは未実装。`cdse-sentinel2`（`sentinel-hub`）は設定だけ置いてある。

**新しいソースを採用する前に、ライセンスを必ず個別に確認すること。** 無雲モザイクは提供元と年次で条件が違う。非商用限定のものを選ぶと将来の選択肢を狭める。

---

## 7. 使えないもの

**Google マップおよび Google Earth の画面キャプチャは、どんな形でも使わない**（仕様 原則1 / §12）。リンクを張るのは問題ないので、結果画面には Google マップへのリンクを置いている。

`npm run validate` は `credit` に `Google` が入っていたら落とす。
