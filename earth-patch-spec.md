# ここはどこ ── EARTH PATCH 仕様書

衛星画像から場所を当てるクイズ。世界地図にピンを打って答える。

- リポジトリ名（案）: `earth-patch`
- 配信: Cloudflare Pages ＋ Cloudflare R2
- 版: 0.1（プロトタイプ）
- ステータス: 未着手

> 名前は仮です。`GROUND TRUTH`（リモートセンシングで「現地検証データ」を指す語）との掛詞にもなるので候補に残してあります。

---

## 1. 何を確かめたいのか

**「予備知識のない人が、3問続けて大きく外しても4問目をめくるか」**

これ一点です。地理に明るい人が楽しめるのは分かりきっているので、検証する意味がありません。知らない人が「分からなかったけど面白かった」と思えるかどうかだけを見ます。

判断の目安。

| 見るもの | 目安 |
| --- | --- |
| 1セッションの中央値の到達問数 | 7問以上（1セット10問） |
| 大きく外した直後に次へ進んだ割合 | 高いほどよい |
| 「解説」を最後まで読んだ割合 | 50%以上（低いなら解説は要らない） |

計測コードは初版には入れません。まず対面か画面共有で五人に触ってもらいます。

---

## 2. 設計の原則

口で「初心者向け」と言うだけでは崩れるので、実装を縛る形にします。

1. **画像は一次配布元の衛星画像のみ。** NASA と Copernicus に限る。Google マップ／Google Earth のキャプチャは、どんな形でも使わない。
2. **回答用の地図に衛星画像を使わない。** 使うと問題画像との見比べだけで解けてしまう。回答用は陸地シルエットと国境だけの線画。
3. **説明のない問題は入れない。** 答えには必ず「なぜここがこう見えるか」を2文で添える。1文目は必ず理由から書く。
4. **文字入力をさせない。** 地名の綴りを知っているかどうかは、このクイズが測りたいものではない。
5. **縮尺は伏せる。** 枠の大きさは強いヒントになる。見たい人だけがヒントとして開く。
6. **難易度は知名度で決める。** 弁別性は「唯一か」の指標であって「答えられるか」の指標ではない（§5）。
7. **ランキングを作らない。** 競技性を入れると、外した人が離脱する方向に力がかかる。確かめたいのは逆のこと。

---

## 3. 用語

| 語 | 意味 |
| --- | --- |
| **問題（question）** | 1枚の衛星画像と、その答えの組。 |
| **枠（frame）** | 画像として切り出す範囲。中心座標と面積（km²）で表す。 |
| **弁別性（distinctiveness）** | その枠が、地球上の他のどことも違う度合い。0〜1。 |
| **知名度（fame）** | 枠内の地物がどれだけ知られているか。0〜1。 |
| **難易度（difficulty）** | 1〜5。知名度から決める。1が易しい。 |
| **候補点（candidate）** | 枠の中心になりうる座標。採用されるとは限らない。 |

「特徴量」は、弁別性や知名度を計算するための**個々の測定値**（水域率、色クラスタ数、Wikipedia の言語版数など）を指します。それらを合成した評価値が弁別性・知名度です。

---

## 4. システム構成

```
                     ┌──────────────────────────┐
  ブラウザ  ────────▶ │ Cloudflare Pages         │  サイト本体
                     │  index.html / css / js   │  数百 KB
                     │  data/questions.js       │
                     │  data/world.js           │
                     └──────────────────────────┘
       │
       └────────────▶ ┌──────────────────────────┐
                     │ Cloudflare R2            │  問題画像
                     │  img.example.com/q/*.jpg │  数百 MB 〜
                     └──────────────────────────┘

  ─────────────────────────────────────────────────────
  手元（Node.js・公開時には動かない）
    Wikidata / Copernicus / NASA  ──▶ scripts/*.mjs ──▶ R2
```

### 4.1 なぜこの構成か

**サイト本体と画像を分ける。** サイト本体は数百KBで、Git で管理して差分を追う価値があります。画像は数百MB〜数GBで、Git に入れると履歴が肥大し、取り直すたびに膨らみます。R2 に置けばこの問題が丸ごと消えます。

**Cloudflare Pages を選ぶ理由。** 無料プランで帯域に上限がなく、Git 連携で push すれば自動デプロイされます。ファイル数上限は1サイト20,000ファイルですが、画像を R2 に出すので、サイト本体は数十ファイルに収まります。

**R2 を選ぶ理由。** 10GB まで無料で、**エグレス（転送量）が無料**です。1枚250KBなら 40,000問まで入ります。容量の心配は事実上なくなります。

### 4.2 R2 の設定で必ずやること

**バケットにカスタムドメインを割り当てる。** `r2.dev` の公開エンドポイントは検証用で、レート制限があり、超えると 429 が返ります。本番では `img.example.com` のような独自サブドメインを R2 バケットに向けてください。ここを忘れると、人が集まった瞬間に画像だけ表示されなくなります。

**バケットは公開読み取り、書き込みは API トークンのみ。** アップロードは手元のスクリプトから S3 互換 API で行います。

### 4.3 ビルドについて

**ビルド工程は持ちません。** 素の HTML / CSS / JavaScript をそのまま配信します。Cloudflare Pages のビルドコマンドには検査だけを置きます。

```
Build command:      npm run check
Build output dir:   .
```

依存パッケージはゼロを原則とします。投影変換、大圏距離、point-in-polygon は、いずれも数十行で書けるので外部ライブラリを入れません。

---

## 5. スコアリング

### 5.1 いちばん大事な考え方

弁別性を「画像の情報量」で定義すると失敗します。熱帯雨林も砂丘も、テクスチャの情報量は極めて高いのに、どのタイルも同じに見えるのでクイズになりません。逆に海岸線は画面の大半が単調な海ですが、一目で分かります。

必要なのは情報量ではなく、**他のどことも違うか**です。

さらに、弁別性が高いだけでは足りません。シベリアの無名の湖は地球上で唯一無二ですが、誰も答えられません。「答えられるか」は画像の性質ではなく**見る人の予備知識**の問題なので、第二の軸として知名度が要ります。

| 軸 | 測るもの | 代理指標 |
| --- | --- | --- |
| 弁別性 | 他と違うか | 全候補の中での最近傍距離ほか（§5.3） |
| 知名度 | 知られているか | Wikipedia の言語版数、枠内人口 |

採用の条件は **弁別性 ≥ しきい値 かつ 知名度 ≥ 下限**。難易度は知名度だけで決めます。

### 5.2 候補点の作り方

陸地全体を格子で刻むと 50km² 単位で約270万タイルになり、全部落とすのは非現実的です（1枚200KBでも540GB）。**画像を落とす前にベクタデータで絞ります。**

初版では Wikidata を起点にします。座標を持つ地物と、その Wikipedia 言語版数が同時に手に入るので、候補点と知名度を1回のクエリで揃えられます。

```sparql
SELECT ?item ?coord (COUNT(DISTINCT ?sitelink) AS ?langs) WHERE {
  ?item wdt:P31/wdt:P279* ?class ;
        wdt:P625 ?coord .
  VALUES ?class { wd:Q23397 wd:Q4022 wd:Q8074 wd:Q39816 wd:Q8502 wd:Q42302 }
  ?sitelink schema:about ?item ; schema:isPartOf/wikibase:wikiGroup "wikipedia" .
}
GROUP BY ?item ?coord
HAVING (?langs >= 5)
```

湖・川・火山・カルデラ・山・ダムなどのクラスに限定し、言語版5以上で絞ると、数千〜1万点程度になります。ここから海洋上の点と南極を落とします。

補助データ。

| データ | 用途 | ライセンス |
| --- | --- | --- |
| Natural Earth 1:110m / 1:50m | 陸地・国境ポリゴン、回答用地図 | パブリックドメイン |
| Wikidata | 候補点と知名度 | CC0 |
| Copernicus DEM GLO-30 | 起伏の計算（任意） | 無償・要出典表記 |

将来的には、名前が付いていないが目を引く場所（塩湖の模様、扇状地、氷河の末端）を拾うために格子走査を足します。初版には入れません。

### 5.3 弁別性の計算

各候補の枠画像を 256px に縮めて、次を計算します。

| 特徴量 | 計算 | 意図 |
| --- | --- | --- |
| 水域率 `w` | 緑と近赤外の比（NDWI 相当）の陽性画素率 | 海岸線・湖の有無 |
| 水域の分散 `wv` | 画像を4×4に割ったときの `w` のばらつき | 全面海／全面陸を落とす |
| 色クラスタ数 `c` | k-means(k=6) の有効クラスタ数 | 地被の多様さ |
| 直線性 `l` | Hough 変換の最大投票値 | 人工物の存在 |
| 最近傍距離 `nn` | 64次元ハッシュでの全候補中の kNN 距離 | **他と違うか** |

合成は暫定でこうします。

```
distinct = 0.45 * norm(nn)
         + 0.25 * norm(wv)
         + 0.20 * norm(c)
         + 0.10 * norm(l)
```

**重みは必ず目視でチューニングします。** 候補から100枚を無作為に抜き、人が「分かる／分からない」の2値でラベルを付け、`distinct` の順位との相関を見て調整します。この作業をやらずに重みを固定すると、まず間違いなく雨林と農地が上位に来ます。

### 5.4 枠のサイズを決める

候補点ごとに、枠を段階的に広げながら弁別性を測り、しきい値を超えた時点で確定します。

```
SIZES = [50, 100, 200, 500, 1000, 2000]   // km²

for (const s of SIZES) {
  const probe = fetchProbe(center, s);      // 48px の下見
  if (isCloudOrVoid(probe)) continue;
  const tile = fetchTile(center, s);        // 本番解像度
  if (distinct(tile) >= T) return { size: s, tile };
}
return null;                                 // 捨てる
```

特徴的な湖なら 50km²（一辺約7km）で足り、砂漠なら 1000km²（一辺約32km）まで広げないと弁別性が立ちません。これが可変枠の目的です。

枠の面積が決まると必要な解像度も決まります。

| 面積 | 一辺 | 1024px 時の分解能 | 取得元 |
| --- | --- | --- | --- |
| 50 km² | 約 7 km | 約 7 m/px | Sentinel-2（10m）※実質限界 |
| 100 km² | 約 10 km | 約 10 m/px | Sentinel-2（10m） |
| 200 km² | 約 14 km | 約 14 m/px | Sentinel-2（10m） |
| 500 km² | 約 22 km | 約 22 m/px | Sentinel-2 / HLS |
| 1000 km² | 約 32 km | 約 31 m/px | Landsat / HLS（30m） |
| 2000 km² | 約 45 km | 約 44 m/px | Landsat / HLS（30m） |

50km² の枠は 10m 分解能だと元が 700px しかありません。**表示サイズは枠に応じて可変**とし、引き伸ばしはしません。

### 5.5 難易度

```
fame       = norm(log(1 + 言語版数))
difficulty = 1 + floor(5 * (1 - fame))     // 1〜5、1が易しい
```

出題は易しい順に並べます。1セット10問なら、難易度 1,1,2,2,2,3,3,4,4,5 のような配分にします。

### 5.6 雲の扱い

このクイズには日付の制約がありません。「その場所がいちばんよく見える一枚」でよいので、**年次の無雲モザイク**を使います。単日のシーンを選んで雲を弾く工程が丸ごと不要になります。

無雲モザイクは提供元と年次でライセンス条件が異なり、非商用限定のものもあります。採用前に必ず個別に確認してください。高緯度では季節の継ぎ目が出ることがあるため、気になる場合は夏季限定のモザイクを選びます。

---

## 6. データ

### 6.1 `data/questions.js`

Frozen Lake Atlas と同じく、JSON ではなくグローバル変数を代入する `.js` にします。`file://` でダブルクリックしても動くためです（`fetch` はローカルファイルだと CORS で弾かれます）。

```js
window.EARTH_PATCH_QUESTIONS = [
  {
    id: "chuquicamata",
    adopted: true,

    answer: {
      lat: -22.30,
      lon: -68.90,
      place: "チュキカマタ銅山",
      region: "チリ・アントファガスタ州",
      country: "CL"
    },

    frame: { areaKm2: 200, sensor: "s2", meters: 10 },

    headline: "砂漠に空いた楕円の穴",
    caption: "山ごと掘り下げてできた世界最大級の露天掘りの銅山です。掘り進んだぶんだけ、壁にらせん状の道が段になって刻まれています。",
    term: "露天掘り",

    scores: { distinct: 0.87, fame: 0.62, difficulty: 3 },

    image: {
      key: "q/chuquicamata.jpg",
      width: 1400,
      date: "2025-08-03",
      credit: "Contains modified Copernicus Sentinel data 2025"
    }
  }
];
```

書き方の規約。

- `headline` は専門用語を使わない。見たままを言う。
- `caption` は2文まで。1文目は必ず「なぜそう見えるか」。
- `term` は本文の外。読まなくても成立させる。
- `adopted` … `true` 採用／`false` 不採用／書かない＝未決定。不採用を書き残さないと、取得のたびに同じ画像が戻ってきます。

**答えの座標はクライアントに入ります。** 開発者ツールを開けば全問の正解が読めますが、ランキングを作らない方針（原則7）なので、これを塞ぐ意味がありません。サーバー側処理を持たない理由でもあります。

### 6.2 `data/world.js`

回答用の世界地図と国判定に使うポリゴン。Natural Earth 1:110m を簡略化したものを埋め込みます。目安は 300〜500KB。

```js
window.EARTH_PATCH_WORLD = {
  land: [ /* 陸地ポリゴンの配列 */ ],
  countries: [ { iso: "CL", name: "チリ", rings: [ /* ... */ ] } ]
};
```

### 6.3 `img/manifest.js`（自動生成・手で編集しない）

R2 にアップロード済みの画像の一覧。取得スクリプトが書き出します。

---

## 7. 画面と操作

### 7.1 出題画面

```
┌──────────────────────────────────────┐
│  EARTH PATCH              3 / 10     │
├──────────────────────────────────────┤
│                                      │
│        （衛星画像・全面）             │
│                                      │
├──────────────────────────────────────┤
│  ここはどこ？          [ ヒント ]     │
│  ┌────────────────────────────────┐  │
│  │      世界地図（線画）           │  │
│  └────────────────────────────────┘  │
│              [ 決定 ]                 │
└──────────────────────────────────────┘
```

- 地図をタップ／クリックするとピンが立つ。打ち直しは何度でも可。
- ピンが立つまで「決定」は無効。
- ヒントは1問につき1回まで。開くと縮尺（「この幅＝東京から横浜まで」）が出る。使ったことは結果画面に表示するが、減点はしない。
- キーボード: `Enter` 決定、`H` ヒント。

### 7.2 結果画面

```
┌──────────────────────────────────────┐
│  ┌────────────────────────────────┐  │
│  │  地図：推測ピン ─── 正解ピン    │  │
│  └────────────────────────────────┘  │
│  大陸は合っています。国はひとつ隣。   │
│  1,340 km ／ 2,780 点                 │
├──────────────────────────────────────┤
│  チュキカマタ銅山                     │
│  チリ・アントファガスタ州             │
│  22.30S 68.90W                        │
│                                      │
│  砂漠に空いた楕円の穴                 │
│  山ごと掘り下げてできた世界最大級の…  │
│  ── 露天掘り と呼ばれています         │
│                                      │
│  [ Google マップで見る ]              │
│  2025-08-03 撮影 / Copernicus …       │
│              [ 次へ ]                 │
└──────────────────────────────────────┘
```

Google マップのリンクは座標から組み立てます。**リンクを張るのは問題ありませんが、Google の画面キャプチャを問題画像に使うことはできません。**

```js
const url = `https://www.google.com/maps/@${lat},${lon},${zoom}z/data=!3m1!1e3`;
```

### 7.3 セッション

- 1セット10問。難易度は易しい順。
- 終了画面に合計点と、外した場所の一覧（サムネイル＋地名）。
- 進捗と既出問題は `localStorage` に保存。使えない環境ではメモリのみで動かし、落とさない。
- 同じ問題が連続セットで出ないよう、直近50問を除外する。

---

## 8. 地図の実装

### 8.1 投影

**正距円筒図法**を使います。Web メルカトルは高緯度を大きく引き伸ばすため、砂漠・シベリア・カナダ北部の出題が多いこのクイズでは、それらの地域だけピンを置くのが不当に易しくなります。

```js
// 表示範囲は緯度 -60〜85（南極を除く）
const x = ((lon + 180) / 360) * W;
const y = ((85 - lat) / 145) * H;
```

ズームは `transform: scale()` と `translate()` だけで実装します。最大8倍。

### 8.2 距離

必ず大圏距離（Haversine）で計算します。地図上のピクセル距離を使うと投影の歪みが点数に乗ります。

```js
function haversine(a, b) {
  const R = 6371, rad = Math.PI / 180;
  const dLat = (b.lat - a.lat) * rad;
  const dLon = (b.lon - a.lon) * rad;   // 経度差は自然に折り返す
  const h = Math.sin(dLat / 2) ** 2 +
            Math.cos(a.lat * rad) * Math.cos(b.lat * rad) * Math.sin(dLon / 2) ** 2;
  return 2 * R * Math.asin(Math.sqrt(h));
}
```

**日付変更線に注意。** 経度 179° と −179° は実際には約220km しか離れていません。経度差を素朴に引くと 358° になるので、`dLon` を ±180° の範囲に正規化してから使います。

### 8.3 得点

```js
const score = Math.round(5000 * Math.exp(-d / 1500));
```

| ずれ | 点数 |
| --- | --- |
| 0 km | 5000 |
| 100 km | 4677 |
| 1,000 km | 2567 |
| 3,000 km | 677 |
| 5,000 km | 179 |

数字だけを返すのは冷たいので、**定性的な帯を併記**します。判定は Natural Earth の国境ポリゴンに対する point-in-polygon（ray casting、約20行）で行います。

| 条件 | 表示 |
| --- | --- |
| 同じ国 | 「国まで当たっています」 |
| 同じ大陸・隣接国 | 「大陸は合っています。国はひとつ隣。」 |
| 同じ大陸 | 「大陸は合っています。」 |
| それ以外 | 「大陸が違いました。」 |

### 8.4 難易度の調整弁

国名ラベルの表示は設定で切り替えられるようにします。初期値は**表示あり**。ラベルがないと、地理に不慣れな人には地図が模様にしか見えません。

---

## 9. ディレクトリ構成

```
.
├── index.html                 画面の骨組み
├── assets/
│   ├── style.css              配色・組版
│   ├── app.js                 出題・採点・進行
│   └── map.js                 投影・描画・point-in-polygon
├── data/
│   ├── questions.js           問題データ（人が書く）
│   └── world.js               陸地・国境ポリゴン（自動生成）
├── img/
│   └── manifest.js            R2 上の画像の一覧（自動生成）
├── scripts/
│   ├── scan-candidates.mjs    Wikidata から候補点を作る
│   ├── score-candidates.mjs   弁別性を計算し、枠サイズを決める
│   ├── fetch-tiles.mjs        画像を取得して R2 へ上げる
│   ├── contact-sheet.mjs      目視用の一覧を書き出す
│   ├── build-world.mjs        Natural Earth から world.js を作る
│   ├── validate.mjs           規約の検査
│   └── lib/
└── docs/
    ├── SCORING.md             弁別性と知名度の詳細
    ├── IMAGERY.md             取得と差し替えの手順
    └── DATA_SCHEMA.md         データの書き方
```

### コマンド

```bash
npm run scan        # Wikidata から候補点を作る
npm run score       # 弁別性を計算し、枠サイズを決める
npm run fetch       # 画像を取得して R2 へ上げる
npm run fetch:dry   # 通信せず、叩く URL だけ確認する
npm run sheet       # 目視用の一覧を書き出す
npm run world       # Natural Earth から world.js を作る
npm run validate    # データの規約違反を検査する
npm run check       # 構文チェック + validate（Pages のビルドコマンド）
npm run serve       # ローカル配信
```

---

## 10. 検査（`validate.mjs`）

Cloudflare Pages のビルドで走るので、**通らなければ公開されません。**

| 検査 | 落とす条件 |
| --- | --- |
| 必須項目 | `id` `answer` `frame` `headline` `caption` `term` `image` のいずれかが欠けている |
| id の重複 | 同じ id が2件以上 |
| 座標 | 陸地ポリゴンの外にある／南極 |
| 文章 | `caption` が3文以上 |
| 文章 | `headline` に `term` の語がそのまま入っている |
| 出典 | `credit` に `Google` を含む |
| 難易度 | `difficulty` が1〜5の整数でない |
| 画像 | `img/manifest.js` に該当キーがない |
| 出題順 | 同じ国が3問連続する |

---

## 11. 容量とコスト

### 見積もり

1枚 1400px の JPEG（品質80）を約 250KB として計算します。

| 問題数 | 画像合計 | 置き場所 |
| --- | --- | --- |
| 100 | 25 MB | どこでも可 |
| 500 | 125 MB | どこでも可 |
| 2,000 | 500 MB | R2 |
| 10,000 | 2.5 GB | R2（無料枠内） |
| 40,000 | 10 GB | R2 無料枠の上限 |

**容量が制約になることは事実上ありません。** 解説文を人が書くほうが先に頭打ちになります（§13）。

### 無料枠の消費

| 項目 | 無料枠 | 見込み |
| --- | --- | --- |
| Pages ビルド | 月500回 | 月20〜50回 |
| Pages ファイル数 | 20,000 | 数十 |
| Pages 帯域 | 実質上限なし | — |
| R2 ストレージ | 10 GB | 数百 MB |
| R2 オペレーション | 月100万 | 1セッション10リクエスト＝月10万セッション相当 |

無料枠に収まります。ただし Cloudflare の Self-Serve 利用規約には、動画などの大きな非HTMLファイルの配信を主目的とする利用への制限があります。通常の Web サイトの画像なら問題になる規模ではありませんが、頭には置いておいてください。

---

## 12. ライセンス

| 対象 | 条件 |
| --- | --- |
| コード | MIT |
| 解説文（`data/questions.js`） | CC BY 4.0 |
| NASA の画像（GIBS / HLS / Landsat） | 米国政府の著作物。出典表記のうえ自由に使える |
| Copernicus Sentinel | `Contains modified Copernicus Sentinel data [年]` を付ければ再配布可 |
| 無雲モザイク | **提供元と年次で条件が違う。採用前に個別確認** |
| Natural Earth | パブリックドメイン |
| Wikidata | CC0 |

**Google マップおよび Google Earth の画面キャプチャは使えません。** リンクを張ることは問題ありません。

出典は取得時に `img/manifest.js` へ記録し、結果画面と `CREDITS.md` の両方に出します。

---

## 13. 進め方

| | 内容 | 目的 |
| --- | --- | --- |
| **M0** | **手で10問作る。** 地図も採点もなし。画像を出して「答え」ボタンで解説が開くだけ | 自分が面白いと思えるか。ここで面白くなければ以降は無駄 |
| M1 | 地図ピンと採点。1セット10問。R2 へ移行 | 「外しても次へ進むか」を五人で確かめる |
| M2 | 取得パイプライン。100問 | 手作業の限界を機械で押し上げる |
| M3 | 弁別性の自動計算と重みの調整。500問 | 選定を機械に任せられるか |
| M4 | テーマ別（水／砂／氷／人工物）、難易度選択 | — |

**M0 を飛ばさないでください。** パイプラインは面白さを作りません。10問を手で作ってみて、自分が退屈なら、500問あっても退屈です。

**律速はコードではなく文章です。** 1問あたり地名の確認と2文の執筆で10〜15分。100問で20時間。弁別性のスコアの役割は「採否を決めること」ではなく、**人が見る順番を並べ替えること**です。数千の候補から人が選ぶのは不可能ですが、上位200枚を眺めるなら現実的です。

---

## 14. やらないこと

意図的に入れません。

- ランキング、対戦、タイムアタック
- ログイン、アカウント
- 文字入力による回答
- 閲覧時のタイル取得（画像は事前に取って R2 に置く）
- 地上写真、ストリートビュー的なもの
- 気候変動や環境問題への言及（記録であって主張ではない）

---

## 付録: 未確定事項

着手前に決める必要があるもの。

1. **無雲モザイクの提供元とライセンス。** 非商用限定のものを選ぶと将来の選択肢を狭めます。
2. **弁別性の重みとしきい値。** 100枚のラベル付けをやってから決めます。
3. **Wikidata のクラス指定。** 自然地物だけにするか、都市や空港も入れるか。人工物を入れると易しくなりますが、砂漠ばかりになるのを防げます。
4. **1セットの問数。** 10問は仮です。M1 で触ってもらって決めます。
5. **国名ラベルの初期値。** 表示ありを仮としていますが、難易度への影響が大きいので実測が要ります。
